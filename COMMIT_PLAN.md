# Suggested Commit Plan

Use these as your 5 meaningful commits:

```bash
git add .
git commit -m "Create Vite TypeScript project"
```

```bash
git add .
git commit -m "Add internship homepage sections"
```

```bash
git add .
git commit -m "Style homepage layout and mobile design"
```

```bash
git add .
git commit -m "Add TypeScript interactions"
```

```bash
git add .
git commit -m "Add README and Week 1 update"
```
